let nombre = prompt('Ingrese su nombre');

let edad = prompt('Ingrese su edad');

if (edad >= 18) {
    alert(`${nombre} es mayor de edad. Aca tenes el carnet de conducir`);
} else {
    alert(`${nombre} es menor de edad. no tenes carnet viejita`);
}
